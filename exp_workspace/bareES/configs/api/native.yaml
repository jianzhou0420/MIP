# api/native — the vendor's own endpoint: Claude Code on its subscription, codex on its login, mini on the provider key.
# This folder is the experiment's Hydra api group: a file beside it picks one with `api=<name>` (defaults: - api: native).
# Every key here lands beside the model id in agent.model (ModelSpec.extra: api_gateway / gateway_task / gateway_script).
api_gateway: null
